{
  "name": "@ts5-nameof/scripts-common",
  "version": "0.0.1",
  "description": "ts5-nameof - Common scripts for ts5-nameof packages.",
  "main": "dist/index.js",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist && tsc --b --clean",
    "build": "tsc",
    "test": "echo 'ignore'",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^5.0.0"
  }
}
