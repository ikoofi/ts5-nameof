{
  "name": "@ts5-nameof/transforms-common",
  "version": "0.0.1",
  "description": "ts5-nameof - Common code for transforms.",
  "main": "dist/index.js",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist && tsc --b --clean",
    "build": "cp -r ./src  ./dist",
    "test": "echo 'ignore'",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@ts5-nameof/common": "^0.0.1"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.0",
    "mocha": "^9.1.3",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^5.0.0"
  }
}
