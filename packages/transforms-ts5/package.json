{
  "name": "@ts5-nameof/transforms-ts5",
  "version": "1.0.0",
  "description": "ts5-nameof - TypeScript compiler transforms for ts5-nameof packages.",
  "main": "./dist/index.js",
  "author": "Kornvv",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist && tsc --b --clean",
    "build": "tsc --b",
    "test": "npm run build && mocha",
    "test:debug": "npm run build && mocha --inspect-brk"
  },
  "dependencies": {
    "@ts5-nameof/common": "^1.0.0",
    "@ts5-nameof/transforms-common": "^1.0.0"
  },
  "devDependencies": {
    "@ts5-nameof/tests-common": "^1.0.0",
    "mocha": "^9.1.3",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
