{
  "name": "@ts5-nameof/tests-common",
  "version": "1.0.2",
  "description": "Common tests for ts5-nameof packages.",
  "main": "dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist && tsc --b --clean",
    "build": "tsc",
    "test": "echo 'ignore'",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@dprint/formatter": "^0.1.5",
    "@dprint/typescript": "^0.60.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
